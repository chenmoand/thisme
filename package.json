{
  "name": "thisme",
  "version": "1.0.0",
  "repository": "https://github.com/chenmoand/thisme.git",
  "author": "chenmo",
  "license": "MIT",
  "private": true,
  "scripts": {
    "web": "cd thisme-blog-react && yarn build:dev && gh-pages -d build",
    "ci": "cd thisme-blog-react && yarn build:prod && cd .. &&node ./script/nci",
    "deploy:yarn": "yarn global add webpack webpack-cli webpack-dev-server && yarn && cd thisme-blog-react && yarn",
    "deploy:npm": "npm install -g webpack webpack-cli webpack-dev-server && npm install && cd thisme-blog-react && npm install"
  },
  "dependencies": {
    "axios": "^0.21.2",
    "gh-pages": "^2.1.1"
  },
  "devDependencies": {}
}
